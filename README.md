# service-dependency-project
A system of interconnected services using a graph representation. Each service may depend on one or more other services. When one service fails, the system determines which other services are affected by traversing the dependency graph.
